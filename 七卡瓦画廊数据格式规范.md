# 七卡瓦画廊数据格式规范

## 1. 数据载体

每张图纸存储为一个 GitHub Issue：

```text
https://github.com/Zippland/perler-beads-gallery/issues/{id}
```

- `id`：GitHub Issue number。
- `createdAt`：GitHub Issue `created_at`。
- 图纸数据：Issue 正文中的 `json:perler-gallery-data` 代码块。
- 图纸不保存 PNG、JPG 或 WebP；预览由客户端根据 `grid` 和 `palette` 绘制。

## 2. 公开图纸结构

```ts
interface PublicGalleryWork {
  v: 1 | 2
  access?: 'public'
  title: string
  width: number
  height: number
  palette: string[]
  keys: string[]
  grid: string
}
```

| 字段 | 类型 | 约束 |
|---|---|---|
| `v` | `1 \| 2` | 数据版本；v1 无 `access` |
| `access` | `'public'` | v2 公开作品必填 |
| `title` | `string` | 非空作品名称 |
| `width` | `number` | 正整数，网格列数 |
| `height` | `number` | 正整数，网格行数 |
| `palette` | `string[]` | HEX 色值数组，元素格式 `#RRGGBB` |
| `keys` | `string[]` | 拼豆色号数组，与 `palette` 同索引 |
| `grid` | `string` | 行优先、每格 2 位十六进制字符 |

## 3. grid 编码

按从左到右、从上到下的顺序，将每格编码为一个字节：

```text
00..fe  palette/keys 数组索引
ff      空白、透明或擦除格
```

第 `i` 格：

```ts
const index = Number.parseInt(grid.slice(i * 2, i * 2 + 2), 16)
const row = Math.floor(i / width)
const col = i % width

const cell = index === 0xff
  ? null
  : {
      hex: palette[index],
      code: keys[index]
    }
```

必须满足：

```text
grid.length === width * height * 2
palette.length === keys.length
grid 仅包含小写或大写十六进制字符
所有非 ff 索引必须小于 palette.length
```

## 4. 公开图纸示例

```json
{
  "v": 2,
  "access": "public",
  "title": "示例图纸",
  "width": 3,
  "height": 2,
  "palette": ["#000000", "#FFFFFF"],
  "keys": ["H07", "H01"],
  "grid": "00ff010100ff"
}
```

解码结果：

```text
H07  空白  H01
H01  H07   空白
```

## 5. 私密图纸结构

```ts
interface PrivateGalleryWork {
  v: 2
  access: 'private'
  title: string
  width: number
  height: number
  palette: string[]
  grid: string
  encryptedKeys: {
    ciphertext: string
    iv: string
    salt: string
    iterations: 100000
    algo: 'PBKDF2-SHA256/AES-GCM-128'
  }
  contact?: string
}
```

私密模式仅加密 `keys`；`grid` 和 `palette` 仍可还原作品画面。

## 6. GitHub Issue 正文格式

````markdown
**示例图纸** | 3x2 | 2 colors

```json:perler-gallery-data
{"v":2,"access":"public","title":"示例图纸","width":3,"height":2,"palette":["#000000","#FFFFFF"],"keys":["H07","H01"],"grid":"00ff010100ff"}
```
````

## 7. 线上读取接口

列表：

```http
GET https://perlerbeads.zippland.com/api/gallery/list?page=1&per_page=20
```

```ts
interface GalleryListResponse {
  works: GalleryListItem[]
  hasMore: boolean
}

interface GalleryListItem {
  id: number
  title: string
  width: number
  height: number
  palette: string[]
  keys?: string[]
  grid: string
  createdAt: string
  access: 'public' | 'private'
  encryptedKeys?: PrivateGalleryWork['encryptedKeys']
  contact?: string
}
```

单张：

```http
GET https://perlerbeads.zippland.com/api/gallery/list?id={id}
```

```ts
interface GalleryDetailResponse {
  work: GalleryListItem
}
```

## 8. 提交接口

```http
POST https://perlerbeads.zippland.com/api/gallery/submit
Content-Type: application/json
```

请求体：

```ts
interface GallerySubmitRequest {
  title: string
  body: string
}
```

- `title`：`{作品名称} - {width}x{height}`。
- `body`：第 6 节定义的 Markdown 正文。

成功响应：

```json
{
  "id": 1655
}
```

## 9. 直接读取 GitHub Issues

```http
GET https://api.github.com/repos/Zippland/perler-beads-gallery/issues?state=open&per_page=100&page=1
```

处理规则：

1. 忽略 Pull Request 条目。
2. 从 `body` 提取 `json:perler-gallery-data` 代码块。
3. 执行 `JSON.parse`。
4. 按第 3 节执行结构校验。
5. 使用 Issue `number` 作为 `id`，使用 `created_at` 作为 `createdAt`。

## 10. 许可边界

`perler-beads-gallery` 仓库无 LICENSE；图纸 JSON 无 `author`、`source`、`license` 字段。公开可读取不等于允许商业复制、修改或再分发。
