# PixelDoodle 功能开发状态

更新日期：2026-08-24
验收口径：Web 优先。✅ 表示当前代码范围已完成；🟡 表示软件链路已实现但仍有明确生产或实机缺口；⏸ 表示尚未实现。原始需求实际包含 12 个子项，以下全部列出。

| 编号 | 原始功能 | 状态 | 当前实现与边界 |
|---|---|---|---|
| 一-1 | 六种固定钉板尺寸 | ✅ | Web、APP/小程序共享工具支持 29×29、32×32、52×52、78×78、104×104、104×74。 |
| 一-2 | 自由像素创作、涂色、撤销重做、缩放、作品保存 | ✅ Web | Web 编辑器支持画笔创作、撤销/重做、缩放、本地保存和恢复；已加入私有云保存入口。 |
| 一-3 | 多套专业拼豆配色方案、一键套用 | ✅ Web | 内置 96、120、144、168、221 色预设，可一键重新量化映射。 |
| 二-1 | 手机相册任意图片导入上传 | ✅ | Web 文件导入及 APP/小程序相册、相机选择已接通。 |
| 二-2 | 网格校准、自由调节大小/缩放/位置 | ✅ Web | 校准网格按目标钉板行列显示，支持拖动、缩放、复位和目标比例裁剪。 |
| 二-3 | 一键 AI 识别并自动适配尺寸生成标准图纸 | 🟡 | 已完成一键本地像素量化、尺寸适配和标准矩阵输出；它是确定性图像算法，不虚称训练型/语义型 AI。若验收必须使用第三方 AI 识别模型，仍需接模型接口。 |
| 三-1 | 全尺寸分类海量拼豆素材库 | ✅ Web | 64,268 份图纸可按来源、中文分类、关键词及六种目标尺寸分页检索；运行时读取服务器素材数据，不把母库打进前端包。 |
| 三-2 | 素材一键套用、自由二次编辑 | ✅ Web | 素材可解码、专业色盘映射、等比缩放居中并进入现有编辑器继续修改。 |
| 四-1 | 扫码支付成功后设备解锁，未支付不可用 | 🟡 | 已有订单状态机、签名回调校验、幂等防重、支付后 51 字节 v2 设备授权、BLE 固件验签、绝对到期时间及持久化单调序号防重放；未实现设备端验签的 Wi-Fi/串口链路在付费锁开启时默认 503 拒绝。本地沙箱可验收；真实微信/支付宝、生产回调域名、逐台设备密钥灌装及 ESP32 实机烧录联调尚未完成。 |
| 五-1 | 作品本地、云端双保存 | 🟡 | 本地保存已完成；服务端私有作品接口、用户隔离、版本冲突、软删除/恢复、SHA-256 完整性及 Web 入口已完成。默认服务端存储为 SQLite；生产账号体系和对象存储/托管数据库尚未接入。 |
| 五-2 | 实时设备状态监测、故障自动预警 | 🟡 | Web BLE 每 10 秒读取固件亮度状态作为心跳，支持离线、ACK/NACK、超时告警、确认及本地历史。尚无云端持续监控，温度、电压、电流等硬件故障也需设备传感器上报后才能覆盖。 |
| 五-3 | 自动分润、收益明细查询与导出 | 🟡 | 已有不可变收益台账、按基点精确分配、自动/手动结算批次、收益分页查询及 UTF-8 CSV 导出，沙箱代付可验收。真实银行/钱包/支付平台代付接口、退款冲正与正式财务规则尚未接入。 |

## 本轮已完成的软件闭环

- 支付与锁机：设备和价格由服务端登记；未支付订单不能领取授权；签名支付回调成功后才生成绑定用户、设备、时长、绝对到期时间、单调序号和随机数的 v2 授权；服务端和固件均维护设备序号高水位，旧授权不能覆盖新授权，固件默认锁定并拒绝未经有效授权的图像/高亮指令。
- 私有云：作品创建、列表、读取、带 `expected_version` 的并发安全更新、软删除和恢复；用户只能访问自己的数据；每个版本保存独立 JSON 并校验 SHA-256。
- 分润：支付入账与订单更新同事务记录；金额按基点整数分配且总额守恒；支持收益状态查询、CSV 导出、结算批次幂等及失败释放。
- Web：个人页已增加“云端与设备中心”，覆盖沙箱支付、设备授权、云作品和收益功能；画布已增加私有云保存。

## 当前验证证据

- 后端：`python -m unittest discover -s tests -v`，66/66 项通过。
- 固件协议静态检查：1/1 项通过；PlatformIO ESP32 固件编译通过，RAM 9.1%（29,692/327,680），Flash 17.7%（555,237/3,145,728）。
- 前端：45 个测试文件、142/142 项通过；H5 与微信小程序生产构建均通过。
- HTTP 端到端：验证服务端价格权威、沙箱 GET 不改订单、未支付拒绝、v2 授权 51 字节及序号递增、旧授权失效、分润金额守恒、CSV 导出、跨用户同作品 ID 隔离、版本冲突、Wi-Fi 管理权限及 Wi-Fi/串口付费锁失败关闭。
- 浏览器端到端：沙箱下单付款、收益展示、画布绘制、私有云保存、编辑后云同步状态失效、再次保存到 v3 均通过；控制台无警告或错误。
- 未计为已验证：真实支付/代付服务、生产账号与对象存储、ESP32 实机烧录、真实 BLE 板卡解锁及生产网络部署。

生产接入契约、环境变量和替换点见 [支付设备锁私有云分润接口接入说明.md](./支付设备锁私有云分润接口接入说明.md)。
