# PixelDoodle 功能开发状态

更新日期：2026-08-25
统一基线：[docs/LATEST_BASELINE.md](./docs/LATEST_BASELINE.md)

验收口径：H5/Tauri Android 优先。✅ 表示当前代码范围已完成；🟡 表示软件链路已实现但仍有明确生产或实机缺口；⏸ 表示尚未实现。原始需求实际包含 12 个子项，以下全部列出。

| 编号 | 原始功能 | 状态 | 当前实现与边界 |
|---|---|---|---|
| 一-1 | 六种固定钉板尺寸 | ✅ | H5/Tauri Android 支持 29×29、32×32、52×52、78×78、104×104、104×74；小程序/iOS 不纳入当前验收。 |
| 一-2 | 自由像素创作、涂色、撤销重做、缩放、作品保存 | ✅ Web | Web 编辑器支持画笔创作、撤销/重做、缩放、本地保存和恢复；已加入私有云保存入口。 |
| 一-3 | 多套专业拼豆配色方案、一键套用 | ✅ Web | 内置 96、120、144、168、221 色预设，可一键重新量化映射。 |
| 二-1 | 手机相册任意图片导入上传 | ✅ | H5 文件选择及 Tauri Android 系统文件入口已接通；小程序/iOS 不纳入当前验收。 |
| 二-2 | 网格校准、自由调节大小/缩放/位置 | ✅ Web | 校准网格按目标钉板行列显示，支持拖动、位置微调、缩放、复位和目标比例裁剪；导入内容等比居中且不溢出。 |
| 二-3 | 一键 AI 识别并自动适配尺寸生成标准图纸 | 🟡 | 已完成单一本地识别链路、尺寸适配和标准矩阵输出；豆子数量直接决定像素分辨率，固定画框内按比例居中留白，生成后支持色号批量校对。它是确定性图像算法，不虚称训练型/语义型 AI。若验收必须使用第三方 AI 识别模型，仍需接模型接口。 |
| 三-1 | 全尺寸分类海量拼豆素材库 | ✅ Web | 服务器母库保留 64,268 份，H5/APK 内置 1,200 份离线精选；支持来源、中文分类、中英文标签、关键词和六种目标钉板检索。 |
| 三-2 | 素材一键套用、自由二次编辑 | ✅ Web | 素材可解码、专业色盘映射、等比缩放居中并进入现有编辑器继续修改。 |
| 四-1 | 扫码支付成功后设备解锁，未支付不可用 | 🟡 | 服务端沙盒订单、签名回调、幂等防重和 51 字节 v2 授权，以及旧 BeadCraft 固件 `0x0B/0x0C/0x0D` 验签代码已完成。新提供的 BLE V1.4 PDF 未定义支付解锁命令，因此 V1.4 真实设备锁机仍未联调；真实支付、回调域名和逐台密钥也未接。 |
| 五-1 | 作品本地、云端双保存 | 🟡 | 本地保存已完成；服务端私有作品接口、用户隔离、版本冲突、软删除/恢复、SHA-256 完整性及 Web 入口已完成。默认服务端存储为 SQLite；生产账号体系和对象存储/托管数据库尚未接入。 |
| 五-2 | 实时设备状态监测、故障自动预警 | 🟡 | 旧 BLE 适配器每 10 秒读取亮度状态作为心跳，支持离线、ACK/NACK、超时告警、确认及本地历史；BLE V1.4 硬件信息响应已有解析层但尚未接 Notify。温度、电压、电流仍需硬件上报。 |
| 五-3 | 自动分润、收益明细查询与导出 | 🟡 | 已有不可变收益台账、按基点精确分配、自动/手动结算批次、收益分页查询及 UTF-8 CSV 导出，沙箱代付可验收。真实银行/钱包/支付平台代付接口、退款冲正与正式财务规则尚未接入。 |

## 本轮已完成的软件闭环

- 支付与锁机：设备和价格由服务端登记；未支付订单不能领取授权；签名支付回调成功后才生成绑定用户、设备、时长、绝对到期时间、单调序号和随机数的 v2 授权；服务端和固件均维护设备序号高水位，旧授权不能覆盖新授权，固件默认锁定并拒绝未经有效授权的图像/高亮指令。
- 私有云：作品创建、列表、读取、带 `expected_version` 的并发安全更新、软删除和恢复；用户只能访问自己的数据；每个版本保存独立 JSON 并校验 SHA-256。
- 分润：支付入账与订单更新同事务记录；金额按基点整数分配且总额守恒；支持收益状态查询、CSV 导出、结算批次幂等及失败释放。
- Web：个人页已增加“云端与设备中心”，覆盖沙箱支付、设备授权、云作品和收益功能；画布已增加私有云保存。
- v14 本地端：应用名统一为“DIY拼豆”；像素图纸导入完成操作指引、选图、网格校准、裁剪、本地识别、结果、色号校对和进入编辑，照片采用单一本地量化链路；颜色套餐、六种钉板和照片配色常驻平铺。豆子数量直接决定分辨率，固定画框内按比例居中留白；生成后支持滑选、页内全选、全选、取消选择、反选、批量改色和删除。断网本地生成、编辑、保存/恢复、PNG/PDF/JSON 导出、素材双列检索/套用及四个底部入口已闭环；本版不提供万相/通义云生成。
- BLE V1.4：PDF 已转为 Markdown；广播、UUID、命令帧、响应、图片/高亮分包和写入分片的上位机纯协议层已完成。

## 当前验证证据

- 后端：`python -m unittest discover -s tests -v`，68/68 项通过。
- 旧设备付费锁固件静态检查：1/1 项通过；此前 PlatformIO ESP32 固件编译通过，当前未用 V1.4 样机复测。
- 前端：52 个测试文件、183/183 项通过；BLE V1.4 独立类型检查通过；H5 生产构建通过。小程序不纳入当前统一基线验收。
- HTTP 端到端：验证服务端价格权威、沙箱 GET 不改订单、未支付拒绝、v2 授权 51 字节及序号递增、旧授权失效、分润金额守恒、CSV 导出、跨用户同作品 ID 隔离、版本冲突、Wi-Fi 管理权限及 Wi-Fi/串口付费锁失败关闭。
- 浏览器端到端：390×844 移动端视口下，像素图纸入口完成选图、网格位置/大小微调、裁剪、本地识别、结果、色号校对和进入编辑器；照片入口完成选图、默认/鲜艳/柔和配色、钉板切换和进入编辑器。29×29 与 104×104 的固定画框同尺寸，单格从 21.517px 降到 6px；104×74 也保持相同画框并居中留白。页内全选、全选、取消选择、反选、批量改色和删除均已点测；浏览器错误/警告日志为空。
- Android：v14.0.4 通用调试 APK 桌面名称为“DIY拼豆”；versionCode 140004、四 ABI、APK v2 签名和 SHA-256 均已验证。
- 未计为已验证：真实支付/代付服务、生产账号与对象存储、BLE V1.4 ESP32 实机烧录、真实板卡解锁及生产网络部署。

生产接入契约、环境变量和替换点见 [支付设备锁私有云分润接口接入说明.md](./支付设备锁私有云分润接口接入说明.md)。
