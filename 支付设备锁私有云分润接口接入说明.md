# 支付、设备锁、私有云、分润接口接入说明

更新日期：2026-08-24

## 1. 当前边界

当前代码已提供可运行的本地业务闭环和可替换接口：沙箱支付 → 签名回调 → 支付入账 → 分润台账 → 设备授权 → BLE 固件限时解锁，以及用户私有作品保存、查询、更新、删除、恢复。

以下内容尚未声称生产完成：微信支付/支付宝等真实支付、真实代付、正式登录系统、云对象存储/托管数据库、逐台设备密钥管理，以及 ESP32 实机烧录联调。生产环境缺少关键配置时会拒绝支付、认证、代付或设备授权，不会自动降级到沙箱。

## 2. HTTP 身份与权限

本地开发默认使用：

```http
X-PixelDoodle-User-Id: user-001
```

生产使用：

```http
Authorization: Bearer v1.<payload>.<hmac-sha256-signature>
```

- `PIXELDOODLE_AUTH_MODE=bearer_hmac`。
- `PIXELDOODLE_AUTH_SECRET` 至少 32 个字符，只保存在登录服务和后端。
- Token 负载必须包含 `sub`、`iat`、`exp`；签名输入为 `v1.<base64url-payload>`。
- 设备登记、自动结算周期等管理接口还需 `X-PixelDoodle-Admin-Key`；对应 `PIXELDOODLE_ADMIN_KEY` 至少 16 个字符。
- 生产环境不得启用 `PIXELDOODLE_ENABLE_SANDBOX`，也不得接受客户端自报用户 ID。

## 3. 支付与设备解锁流程

1. 管理端调用 `POST /api/commerce/devices` 登记设备、商户账户、服务价格和币种；价格以后端登记值为准。
2. 用户调用 `POST /api/commerce/orders`，提交 `device_id` 和唯一 `idempotency_key`。
3. 后端调用支付适配器创建预支付单，前端使用返回的 `checkout_url` 或 `qr_payload` 展示支付入口。
4. 支付平台回调 `POST /api/commerce/webhooks/{provider_name}`；只有验签/解密成功且订单号、平台单号、金额、币种一致时才转为 `paid`。重复事件幂等处理。
5. 用户调用 `POST /api/commerce/orders/{order_id}/device-grant`。未支付、非本人、错误设备或已过期授权均拒绝。
6. 前端先通过 BLE 发送授权并收到“已解锁”，再发送图案；成功后可调用 `POST /api/commerce/device-activations` 留审计记录。

主要业务接口：

| 方法与路径 | 权限 | 用途 |
|---|---|---|
| `GET /api/commerce/config` | 无 | 查询支付提供方、沙箱状态、自动结算和授权时长。 |
| `POST /api/commerce/devices` | 管理员 | 生产设备登记/更新。 |
| `GET /api/commerce/devices` | 用户 | 查询当前账户名下设备。 |
| `POST /api/commerce/orders` | 用户 | 创建幂等支付订单。 |
| `GET /api/commerce/orders/{order_id}` | 用户 | 查询本人订单状态。 |
| `POST /api/commerce/webhooks/{provider_name}` | 支付平台 | 支付回调；由适配器验签，不依赖用户 Token。 |
| `POST /api/commerce/orders/{order_id}/device-grant` | 用户 | 已支付订单领取限时设备授权。 |
| `POST /api/commerce/device-activations` | 用户 | 记录授权已送达设备。 |

### 3.1 真实支付适配器契约

实现 `core/commerce/payment_provider.py` 中的 `PaymentProvider`：

```python
class PaymentProvider(Protocol):
    name: str
    def create_checkout(self, request: CheckoutRequest) -> CheckoutSession: ...
    def verify_webhook(
        self, headers: Mapping[str, str], raw_body: bytes
    ) -> VerifiedPaymentEvent: ...
```

- `CheckoutRequest`：`order_id`、`amount_cents`、`currency`、`description`、`expires_at`。
- `CheckoutSession`：`provider_order_id`、`checkout_url`、`qr_payload`。
- `VerifiedPaymentEvent`：`event_id`、`order_id`、`provider_order_id`、`event_type`、`amount_cents`、`currency`、`raw_payload`。
- 适配器负责平台证书/公钥验签、报文解密、事件类型归一化；不得仅信任客户端“支付成功”。
- 实现后须在 `build_default_business_services()` 注册实例并把 `PIXELDOODLE_PAYMENT_PROVIDER` 设为其 `name`。仅设置环境变量不会自动安装第三方 SDK，也不会自动注册适配器。
- 若支付平台要求特定回调确认报文，需要在 FastAPI 路由层按该平台规范返回确认内容。

### 3.2 设备授权与 BLE 契约

授权原始数据固定 51 字节，再以无填充 Base64URL 返回给前端：

| 偏移 | 长度 | 含义 |
|---|---:|---|
| 0 | 1 | 版本，固定 `2`。 |
| 1 | 6 | 12 位 HEX 设备码。 |
| 7 | 4 | 解锁时长秒数，`uint32` 小端。 |
| 11 | 8 | 绝对到期时间 `not_after_epoch`，Unix 秒，`uint64` 小端。 |
| 19 | 8 | 设备授权单调序号 `grant_sequence`，`uint64` 小端。 |
| 27 | 8 | 随机 nonce。 |
| 35 | 16 | `HMAC-SHA256(body)` 前 16 字节。 |

BLE 控制字：`0x0B` 开始、`0x0C` 分片、`0x0D` 提交、`0x0E` 查询锁状态；设备以 `0x27, status` 回复。`status=1` 才允许继续发图，`0/2/3/4/5/6/7` 分别表示锁定、格式错、设备错、签名错、重放、密钥缺失、授权过期。

当前 `StaticDeviceSecretProvider` 读取 `PIXELDOODLE_DEVICE_ACTIVATION_SECRET`；固件编译宏 `BEADCRAFT_DEVICE_SECRET` 必须使用同一密钥。生产建议实现 `DeviceSecretProvider.secret_for(device_id)`，从 KMS/HSM 读取逐台不同密钥，并在安全烧录阶段写入设备。后端密钥不得下发给前端。

服务端按设备原子递增并持久化 `grant_sequence` 高水位，只允许当前最高序号授权送达；新授权生成后，同设备旧授权立即失效。固件验签成功后持久化最高序号：低序号和过期后/重启后的同序号均拒绝；当前解锁期间收到同序号仅幂等回复成功，不延长解锁时间。因此 `A → B → A` 重放不能重新解锁。

固件仅在系统时钟达到可信 Unix 时间阈值时校验 `not_after_epoch`，所以生产设备应先通过 NTP、受信主机或安全时钟同步时间；服务端无论如何都会检查数据库授权状态、用户、设备、送达窗口、绝对到期时间和序号高水位。对更高安全级别的联网设备，仍建议增加服务端在线挑战。

### 3.3 Wi-Fi 与串口边界

- 当前仓库的设备端授权验签实现于 BLE 固件。`PIXELDOODLE_DEVICE_LOCK_ENABLED=true` 时，串口发送和高亮接口固定返回 503，防止绕过付费锁。
- Wi-Fi 设备登记和列表只允许管理员访问；IP 必须是 RFC1918 私网 IPv4，设备码必须已存在且启用。
- Wi-Fi 接收端未部署同等授权验签前，保持 `PIXELDOODLE_WIFI_DEVICE_AUTH_ENABLED=false`，Wi-Fi 图案和高亮接口会返回 503。只有接收端已经校验签名、绝对到期时间和设备序号高水位后，才能把该变量设为 `true`。

## 4. 用户私有云

主要接口：

| 方法与路径 | 用途 |
|---|---|
| `POST /api/cloud/works` | 创建作品。 |
| `GET /api/cloud/works` | 分页列出本人作品。 |
| `GET /api/cloud/works/{id}` | 读取本人作品。 |
| `PUT /api/cloud/works/{id}` | 使用 `expected_version` 更新，冲突返回 409。 |
| `DELETE /api/cloud/works/{id}?expected_version=N` | 软删除。 |
| `POST /api/cloud/works/{id}/restore` | 使用 `expected_version` 恢复。 |

画布 JSON 为 `pixeldoodle.canvas/v1`：

```json
{
  "schema_version": "pixeldoodle.canvas/v1",
  "grid_size": { "width": 2, "height": 1 },
  "pixel_matrix": [[null, "A01"]],
  "color_summary": [{ "code": "A01", "count": 1 }],
  "total_beads": 1,
  "palette_preset": "mard-96",
  "editor_state": {}
}
```

每行长度必须等于 `width`、行数必须等于 `height`，`total_beads` 必须等于非空单元格数；单份 JSON 最大 2 MiB。服务端按用户隔离，版本对象不可变，并以 SHA-256 验证完整性。

默认适配器把元数据和 JSON 对象都存入 `PIXELDOODLE_BUSINESS_DB_PATH` 指向的 SQLite。接正式云服务时：

- 元数据实现 `WorkRecordStore`，必须保持用户所有权过滤和 compare-and-swap 版本更新。
- 对象存储实现 `WorkObjectStore.put_json/get_json/delete`，对象键按版本唯一且不可覆盖。
- 用新适配器构造 `PrivateCloudService(records, objects)`；仅修改数据库路径不会自动接入 OSS/COS/S3。

## 5. 分润、收益和代付

支付订单确认成功时，同一数据库事务写入不可变收益订单、分润规则快照和分配明细。默认商户比例为 `PIXELDOODLE_MERCHANT_SHARE_BPS=8000`（80%），剩余部分进入 `PIXELDOODLE_PLATFORM_ACCOUNT_ID`。所有比例使用基点，整数分配后各方金额之和严格等于订单实收金额。

| 方法与路径 | 权限 | 用途 |
|---|---|---|
| `GET /api/commerce/earnings` | 用户 | 按币种、状态分页查询本人收益。 |
| `GET /api/commerce/earnings/export.csv` | 用户 | 导出本人收益，UTF-8 BOM，并防电子表格公式注入。 |
| `POST /api/commerce/settlements` | 用户 | 创建并执行本人幂等结算批次。 |
| `GET /api/commerce/settlements/{batch_id}` | 用户 | 查询本人结算批次。 |
| `POST /api/commerce/settlements/run-cycle` | 管理员 | 先重试陈旧 `processing` 批次，再扫描未结算账户；`retry_stale_after_seconds` 默认 300。 |
| `POST /api/commerce/settlements/retry-processing` | 管理员 | 仅重试陈旧批次；请求可传 `stale_after_seconds`（默认 300）和 `limit`。 |

真实代付实现 `core/commerce/settlement.py` 中的 `PayoutProvider`：

```python
class PayoutProvider(Protocol):
    name: str
    def submit(self, request: PayoutRequest) -> PayoutResult: ...
```

- `PayoutRequest` 包含 `batch_id`、`idempotency_key`、`account_id`、`currency`、`amount_cents` 和订单明细；客户端结算幂等键在服务端按账户隔离，相同字符串可由不同账户安全使用。
- 提交必须使用 `request.idempotency_key` 保证外部代付幂等。
- 返回状态仅允许 `succeeded`、`pending`、`failed`；异步结果通过 `SettlementService.apply_provider_event(...)` 更新。
- 若代付请求超时或适配器抛异常，批次保持 `processing` 并继续占用原收益，避免未知结果时另建批次重复出款；异常摘要记录在 `failure_reason`。调度重试必须复用该批次原 `idempotency_key`，代付平台也必须按此键返回同一笔结果。
- `run-cycle` 可由独立定时任务调用；生产环境支付 webhook 不同步发起外部代付。用户再次以相同结算幂等键提交，也会恢复原批次而不是创建新批次。
- 实现后注入 `SettlementService(payout_provider=...)` 并设置 `PIXELDOODLE_PAYOUT_PROVIDER`。仅设置环境变量不会自动连接银行或钱包。
- 正式代付未接通前保持 `PIXELDOODLE_AUTO_SETTLE=false`；生产进程会强制关闭支付回调内自动结算，应由受控定时任务调用管理员周期接口。真实退款还需按财务规则增加冲正/退款台账，当前没有把退款当作已完成。

## 6. 生产环境变量

| 变量 | 生产要求 |
|---|---|
| `PIXELDOODLE_ENV` | 推荐为 `production`；`prod` 也按生产环境强制执行安全策略。 |
| `PIXELDOODLE_AUTH_MODE` | `bearer_hmac`，或改造为正式身份提供商适配器。 |
| `PIXELDOODLE_AUTH_SECRET` | 至少 32 字符，使用密钥管理服务保存。 |
| `PIXELDOODLE_ADMIN_KEY` | 至少 16 字符；管理接口使用。 |
| `PIXELDOODLE_PAYMENT_PROVIDER` | 已在代码注册的真实支付适配器名称。 |
| `PIXELDOODLE_PUBLIC_BASE_URL` | 支付平台可访问的 HTTPS 公网域名。 |
| `PIXELDOODLE_DEVICE_ACTIVATION_SECRET` | 当前静态密钥方案必需；生产更推荐逐设备 KMS 适配器。 |
| `PIXELDOODLE_DEVICE_SESSION_SECONDS` | 30–3600，默认 900。 |
| `PIXELDOODLE_DEVICE_LOCK_ENABLED` | 必须为 `true`。 |
| `PIXELDOODLE_WIFI_DEVICE_AUTH_ENABLED` | Wi-Fi 接收端未完成同等授权验签前必须为 `false`；此时付费锁下 Wi-Fi 命令会 503 拒绝。 |
| `PIXELDOODLE_BUSINESS_DB_PATH` | 指向持久卷；多实例生产应替换为支持事务的共享数据库适配器。 |
| `PIXELDOODLE_PAYOUT_PROVIDER` | 已在代码注入的真实代付适配器名称。 |
| `PIXELDOODLE_MERCHANT_SHARE_BPS` | 1–9999；上线前由财务确认。 |
| `PIXELDOODLE_PLATFORM_ACCOUNT_ID` | 平台收益账户 ID。 |
| `PIXELDOODLE_AUTO_SETTLE` | 真实代付、限额、审核和风控完成前必须为 `false`。 |

生产环境必须禁用/删除 `PIXELDOODLE_SANDBOX_PAYMENT_ENABLED` 和 `PIXELDOODLE_SANDBOX_PAYMENT_SECRET`。

## 7. 对接方需要提供的资料

- 支付：服务商（微信/支付宝等）、商户号/应用 ID、证书或密钥、SDK 版本、支付产品类型、回调域名和回调样例。
- 登录：用户 ID 规范、Token 签发/刷新方式、过期时间、账号与商户账户的映射。
- 云存储：数据库/对象存储产品、区域、桶名、访问方式、加密、备份和保留策略。
- 设备：12 位设备码清单、逐台密钥灌装方案、固件烧录方式、真实 BLE 板卡和串口日志。
- 分润/代付：账户类型、正式比例、最低结算额、周期、手续费、退款/撤销/争议处理、代付回调和对账文件格式。

收到上述资料后，只需实现并注册对应适配器；现有订单、授权、用户隔离、台账、查询和导出业务层无需重写。
